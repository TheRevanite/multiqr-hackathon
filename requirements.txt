# Python dependencies for the MED1C Multi-QR Code Recognition project
# torch and torchvision must be installed separately to specify CUDA version!

# Core computer vision
opencv-python>=4.8.0
Pillow>=10.0.0

# Dataset and utilities
PyYAML>=6.0
tqdm>=4.65.0

# QR code decoding (assumed local library, not external API)
pyzbar>=0.1.9

# Advanced augmentation library
albumentations>=1.3.0